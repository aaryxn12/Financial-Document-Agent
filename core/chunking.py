"""
Splits extracted document text into overlapping chunks for embedding.
Reuses the chunk_text(text, chunk_size, overlap) logic from Phase 2
topic 4, with production-sensible default sizes (not the tiny
illustrative sizes used during practice).
"""
